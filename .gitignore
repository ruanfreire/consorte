node_modules
dist
.DS_Store
*.local
server/.env
.cursor/

# Overrides locais (não versionar chaves reais aqui)
# .env.production versionado = demo/mock; use .env.production.local para build real local.
