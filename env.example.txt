# Sem ficheiros .env — ajuste `src/config.js` (`MOCK_CONFIG`) para URL pública, countdown, etc.
# Mensagens à Ana: SQLite no browser (sql.js + WASM) com persistência em IndexedDB — sem servidor de BD remoto.
