# Sem ficheiros .env — tudo em `src/config.js` (`MOCK_CONFIG`).
# Firebase Web: Project settings → Your apps.
# Data Connect: `dataconnect/dataconnect.yaml` + deploy (`firebase deploy --only dataconnect`).
# Codelab: https://firebase.google.com/codelabs/firebase-dataconnect-web?hl=pt-br
# SDK Web: https://firebase.google.com/docs/data-connect/web-sdk
