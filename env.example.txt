# Front (Vite) — copie para .env.local (dev) ou GitHub Actions → Secrets (build).
# Build de produção gera a pasta `docs/` (GitHub Pages: pasta `/docs` ou GitHub Actions).

# --- GitHub Pages
# (A) Recomendado: Settings → Pages → Source = "GitHub Actions"
# (B) Branch + pasta /docs: "npm run build" com .env.production.local (Firebase) ou "npm run build:mock" (só demo)

# true = mensagens só no browser (sessionStorage). Produção: false (ver .env.production).
# VITE_USE_MOCK=true

# Contagem regressiva (produção). ISO com fuso, ex.:
# VITE_LAUNCH_AT=2026-04-15T19:00:00-03:00

# URL pública do site SEM barra final (meta og:url / og:image).
VITE_PUBLIC_SITE_URL=

# VITE_PREVIEW_ULTIMA=true

# --- Firebase Firestore (coleção ana_messages)
# Campos: text, imageBase64, createdAt, updatedAt (timestamps). Regras: firestore.rules
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
