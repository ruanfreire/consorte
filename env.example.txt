# Front (Vite) — copie para .env.local ou GitHub Actions → Secrets (build).
# O build de produção gera a pasta `docs/` (GitHub Pages só publica `/` ou `/docs`, não `dist`).

# --- Supabase (O QUE USAR — leia com atenção)
# O app NÃO usa login/senha do banco nem a connection string PostgreSQL (postgres://...).
# No painel Supabase: Project Settings → API
#   • Project URL     → VITE_SUPABASE_URL   (ex.: https://abcdefgh.supabase.co)
#   • anon public key → VITE_SUPABASE_ANON_KEY (JWT longo, começa com eyJ...)
#
# npm run dev  → variáveis em .env.local (NÃO só .env.production.local; o Vite em dev não carrega .env.production)
# npm run build → .env.production.local OU secrets no GitHub Actions (deploy com build no CI)

# --- GitHub Pages (importante)
# Para countdown + Supabase no ar, use UMA destas opções:
#
# (A) Recomendado: Settings → Pages → Build and deployment → Source = "GitHub Actions"
#     O workflow ".github/workflows/deploy.yml" faz o build com os secrets (Supabase, data, etc.).
#     Não depende da pasta docs/ do repositório para publicar.
#
# (B) Deploy pela branch + pasta /docs:
#     • Produção real (Supabase): "npm run build" com .env.production.local (VITE_SUPABASE_*, etc.) e commit docs/
#     • Sem secrets (demo/mock): "npm run build:mock" — usa .env.production.mock; mensagens só no browser (sessionStorage)

# true = sem Supabase; mensagens só no sessionStorage (demo). Produção: deixe false (já no .env.production).
# VITE_USE_MOCK=true

# Contagem regressiva só em produção (npm run build). ISO com fuso, ex.:
# VITE_LAUNCH_AT=2026-04-15T19:00:00-03:00

# URL pública do site SEM barra final (GitHub Pages). Usada nas meta tags og:url e og:image.
# Ex.: https://seu-usuario.github.io
VITE_PUBLIC_SITE_URL=

# Se true, permite abrir ?ultima=1 na URL (última tela + mensagens) no build de preview/produção.
# Em dev não precisa — npm run dev já permite.
# VITE_PREVIEW_ULTIMA=true

# --- Mensagens (escolha UM backend remoto, ou mock)
# VITE_MESSAGES_BACKEND=auto     # padrão: Firestore se VITE_FIREBASE_* completo; senão Supabase
# VITE_MESSAGES_BACKEND=firestore
# VITE_MESSAGES_BACKEND=supabase

# Supabase — rode sql/ana_messages.sql no painel (RLS append-only).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Firebase Firestore — Project settings → Your apps → Web (config object).
# Cole firestore.rules no Console → Firestore → Regras. Coleção: ana_messages
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# --- Servidor Mongo (pasta server/) — opcional, não usado pelo app de mensagens
# MONGODB_URI=mongodb+srv://...
# CORS_ORIGIN=https://seu-usuario.github.io,http://localhost:5173
