# Ligação Supabase: editar `src/config.js` → `MOCK_CONFIG` (VITE_SUPABASE_URL e chave publicável).
# Não é necessário ficheiro `.env` para este projeto.

# Build estático (saída em `docs/`, usada pelo GitHub Pages no repositório):
#   npm run build
#   npm run deploy   (igual ao build — correr antes de commit/push quando quiseres `docs/` atual no git)
# O workflow .github/workflows/deploy.yml também corre `npm run build` no push a main.

# Preview em dev (localhost):
#   ?ultima=1 — abre na última mensagem da história (com VITE_PREVIEW_ULTIMA ou só em localhost).
#   ?countdown=1 — mostra o ecrã de contagem regressiva (só se VITE_LAUNCH_AT ainda for no futuro).
#   ?book=1 — abre a vista “livro” (história completa + chat das mensagens à Ana).
