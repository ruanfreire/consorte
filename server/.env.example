# Copie para server/.env (não commitar). String do Atlas: Database → Connect → Drivers.
MONGODB_URI=mongodb+srv://USUARIO:SENHA@cluster.../consorte?retryWrites=true&w=majority

# Opcional: origens permitidas no CORS (separadas por vírgula). Em dev pode omitir.
# CORS_ORIGIN=https://ruanfreire.github.io,http://localhost:5173

PORT=3001
